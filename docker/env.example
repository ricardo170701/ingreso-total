APP_NAME="Escáner Total"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8181

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=escaner_total
DB_USERNAME=escaner_user
DB_PASSWORD=root

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=public
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MAIL_MAILER=log
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@localhost"
MAIL_FROM_NAME="${APP_NAME}"

# Swagger
L5_SWAGGER_CONST_HOST=${APP_URL}

# Raspberry API
DEVICE_KEY=abcd1234

# Protocolo de Emergencia (Apertura de Puertas)
DOOR_API_KEY=cambia_esto_por_una_clave_secreta
DOOR_EMERGENCY_PORT=8000

